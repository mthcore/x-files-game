## Summary

A short description of what this PR changes.

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Documentation only
- [ ] Refactor (no behavior change)
- [ ] Format finding (decoder grammar change)

## Checklist

- [ ] I have signed off my commits (`git commit -s`).
- [ ] `cd python && pytest -v` passes locally.
- [ ] `cd cpp && cmake -S . -B build && cmake --build build && ctest --test-dir build` passes locally (if I touched C++).
- [ ] `python tools/validate_release.py` passes (if I changed structure).
- [ ] I have **not** attached the game binary to this PR.
- [ ] Documentation updated (if behavior changed).

## Related issues

Fixes #...

## Notes for reviewers

(Anything subtle or worth pointing out)
